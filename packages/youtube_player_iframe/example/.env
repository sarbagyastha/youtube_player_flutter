# Configuration for Flutter Web to use HTML renderer by default
# This avoids CanvasKit CDN loading issues and improves stability

# Set environment variable
FLUTTER_WEB_USE_SKIA=false

# Alternative: Add to your build scripts
# flutter run -d chrome --dart-define=FLUTTER_WEB_USE_SKIA=false
# flutter build web --dart-define=FLUTTER_WEB_USE_SKIA=false

# For VS Code launch.json, add:
# "args": ["--dart-define=FLUTTER_WEB_USE_SKIA=false"]